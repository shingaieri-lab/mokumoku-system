# IS進捗管理ツール（ダンドリワーク）UXレビュー

レビュー日：2026-03-23　／　最終更新：2026-03-24

---

## このツールは何をするもの？

営業チームが「どの会社に連絡したか」「次はいつ連絡するか」を管理するツール。
AIが通話メモを読んで次の行動を提案したり、Googleカレンダーの空き時間を自動で探したりできる。

---

## 1. 使いやすさ（クリック数）

### ✅ うまくいっているところ

- リード新規登録：**3クリック**で完了
- 設定画面を開く：**3クリック**

### ❌ 改善が必要なところ

**候補日を探してメールを送るまでの流れが長すぎる（8〜10クリック）**

```
①候補日ページを開く
②検索条件を入力する
③「検索」ボタンを押す
④空き時間を3つ選ぶ
⑤「メールに使う」ボタンを押す　← ページが切り替わる
⑥メールページでもう一度リードを選ぶ　← ❌ さっき選んでいるのに！
⑦テンプレートを選ぶ
⑧「Gmail下書き保存」を押す
```

**⑥がムダ。** 候補日ページで選んだリード情報をメールページに引き継ぐべき。

---

## 2. 見た目の一貫性

### ❌ AIページだけ見た目が全然違う

| ページ | 背景・テキスト |
|--------|--------------|
| ダッシュボード・リード管理など | 白背景＋緑文字 |
| **AIページだけ** | 真っ暗な紺色背景＋白文字 |

「AIアシスタント」タブを押した瞬間に別のアプリのような見た目になる。
初めて使うユーザーが「バグ？」と感じる可能性がある。
他のページと同じ白系背景に統一、またはわずかな差にとどめるのが望ましい。

### ❌ ナビのラベルが小さすぎる

| 場所 | フォントサイズ |
|------|-------------|
| デスクトップ サイドバー | **9.5px** |
| スマホ ボトムナビ | **8px** |

一般的なWebの最小推奨は12px。8pxはほぼ読めない大きさ。
アイコンで判断させる意図であれば、ラベルを非表示にするか、最低11〜12pxに拡大すべき。

---

## 3. コードの問題（今後の開発に影響）

### ✅ Gmail下書き保存のコードが2か所に重複（約80行）　← **対応済み**

`EmailPage` の `saveGmailDraft` と `AIPage` の `saveAiGmailDraft` を共通関数に統合。
（対応コミット：`998803f`）

### ✅ 祝日リストが2か所に重複定義　← **対応済み**

グローバルの `JP_HOLIDAYS` を AIPage でも参照するよう修正。祝日追加が1か所で完結するようになった。
（対応コミット：`998803f`）

### ✅ メールテンプレートの初期値が2か所で微妙にズレている　← **対応済み**

テンプレート初期値を1か所に統一。
（対応コミット：`998803f`）

### ❌ 単一HTMLファイル（行数は更新後も大規模）

全ロジック・スタイル・テンプレートが1ファイルに集中しており、機能追加・バグ修正の難易度が高い。
長期的にはコンポーネントごとにファイル分割を検討する。

---

## 4. セキュリティの問題

### ✅ パスワードが暗号化なしで保存されていた　← **対応済み**

`bcryptjs` によるハッシュ化・不正アクセス対策・招待コード制を実装。
平文保存のリスクが解消された。
（対応コミット：`ac1bdfb`）

---

## 5. AIページ：解析後の導線が不明

解析→保存した後、次に何をすればいいかの案内がない。

**現状：**
```
解析する → 保存完了！ ← ここで終わり
```

**改善案：**
```
解析する → 保存完了！
           → 「📧 フォローメールを送る」ボタン
           → 「📅 商談候補日を探す」ボタン
```

---

## 6. 設定の初回セットアップが難しすぎる

Google Calendar 連携を開始するまでの手順：

1. Google Cloud Console でプロジェクト作成
2. Calendar API を有効化
3. APIキーを取得
4. OAuth クライアントID を作成
5. 承認済みオリジンの設定
6. 各担当者のカレンダーID を収集
7. アプリの設定画面に全部入力

現状は文字の説明だけで案内しており、IT知識のないユーザーには高いハードル。

- **改善案：** ステップ1〜7を順に案内する「ウィザード形式」の画面を作る

---

## 7. 確認ダイアログがブラウザのポップアップ

削除確認に `window.confirm`（ブラウザ標準のグレーのポップアップ）を使用。
アプリのデザインと統一感がなく、ユーザーが戸惑う。

- **改善案：** アプリ内モーダル（ダイアログ）で確認を取る

---

## 8. 良い点

- KPIカードの視認性が高く、数値が一目でわかる
- LeadCombobox のインクリメンタルサーチ（入力しながら絞り込む）が使いやすい
- モバイル対応がされていてボトムナビも直感的
- エラーバウンダリが実装されており、予期しないエラーに強い
- AI解析結果（興味度・トークポイント・ネクストアクション日）が実用的
- 候補日検索の曜日・バッファ・対象外時間帯の設定が柔軟

---

## 優先度まとめ

### ✅ 対応済み

| 問題 | 対応内容 | コミット |
|------|---------|---------|
| パスワード平文保存 | bcryptjs ハッシュ化・招待コード制を実装 | `ac1bdfb` |
| Gmail保存コードの重複 | 共通関数に統合 | `998803f` |
| 祝日リストの重複定義 | グローバル変数に一本化 | `998803f` |
| テンプレート初期値のズレ | 1か所に統一 | `998803f` |

### 今後対応が必要なもの

| 優先度 | 問題 | なぜ重要か |
|--------|------|-----------|
| 🟠 近いうち | 候補日→メールでリードが引き継がれない | 操作が無駄に増える（6クリック→ムダなリード再選択） |
| 🟠 近いうち | AIページのテーマが他と断絶（紺色背景） | 初見ユーザーがバグと感じる |
| 🟠 近いうち | ナビラベルが小さすぎる（8〜9.5px） | 推奨最小は12px、実質読めない |
| 🟡 余裕があれば | AI解析保存後の次アクション案内がない | 次に何をすべきか迷う |
| 🟡 余裕があれば | 確認ダイアログがブラウザのポップアップ | デザインと統一感がない |
| 🟡 余裕があれば | Google Calendar 設定のウィザード化 | IT知識のないユーザーには手順が多すぎる |
| 🟡 長期的に | 単一HTMLファイルの分割 | 将来の保守性向上 |
